apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
- namespace.yaml
- karpenter/gpu-nodeclass.yaml
- karpenter/gpu-nodepool.yaml
# KEDA resources - uncomment when KEDA/Kedify is installed
# - keda/scaledobject.yaml
# - keda/otel-collector.yaml
# - keda/otel-rbac.yaml
# Load testing - apply separately with: kubectl apply -f kubernetes/k6-load-test.yaml
# - k6-load-test.yaml
- vllm/deployment.yaml
- api/deployment.yaml
- frontend/deployment.yaml
- frontend/ingress.yaml
